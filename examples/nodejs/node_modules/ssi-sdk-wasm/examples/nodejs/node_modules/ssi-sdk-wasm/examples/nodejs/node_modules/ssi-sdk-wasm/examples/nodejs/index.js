import SSI from '../../dist/index.js';

// console.log(SSI.makeDID());
// console.log(SSI.resolveDID("abc"));

function delay(time) {
    return new Promise(resolve => setTimeout(resolve, time));
} 

async function run() {
    await delay(1000);
    console.log('This printed after about 1 second');
    console.log(SSI)
    console.log(SSI.makeDid());
}

run();